// 1. Create a variable with var. Store the string "Hello" in this variable.
var greeting = "Hello";

// 2. Create a variable with let named target. Store the string "World" in this variable.
let target = "World";

// 3. Create a variable named punctuation with const. Store the string "!" in this variable.
const punctuation = "!";

function logVariables() {
  // 4. Log each variable to console with one console.log statement.
  // Example output:
  // > Hello World !
  console.log(greeting, target, punctuation);
}
